# Phase 02: Verification

Status: ⬜ Pending

## Objective

Verify the multi-level sorting works as expected.

## Requirements

- [ ] Create a unit test for `generate_sort_paths` with `output_folder`.
- [ ] Manual test verification.

## Implementation Steps

1. [ ] Create `tests/test_multilevel_sort.py`.
2. [ ] Run tests using `pytest`.

## Files to Create

- `tests/test_multilevel_sort.py`

## Test Criteria

- [ ] Test passes for single level.
- [ ] Test passes for multi-level nested structure.
